export interface Product {
    id: string
    cover: string
    name: string
    rate: number
    price: number
    slug: string
    rateCount: number
    discountPercent: number
    realPrice: number,
    detail: string[]
}